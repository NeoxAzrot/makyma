<%= render 'header',
    title: 'Propose une alternative',
    text: 'Nous sommes constamment à la recherche de nouvelles alternatives. Si tu n’as pas trouvé ce que tu cherchais, fais-le nous savoir, nous la rajouterons au plus vite 🤞',
    large: '???'
%>

<div class="container">
  <div class="row offset-md-2">
    <div class="col-md-8">

      <h2>Écris-nous ton idée :</h2>
      <p>Tous les champs marqués d’un <span class="makyma-orange">*</span> sont obligatoires. </p>
			<%= simple_form_for SuggestedAlternative.new,  html: { class:"suggest-form"} do |f| %>
				<%= f.error_notification %>
				<%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

				<div class="form-inputs">
					<%= f.association :product, label: 'Produit', class: 'deroulant-action' %>
					<%= f.input :title, label: "Nom de l'alternative" %>
					<%= f.input :description, label: "Description de ton alternative"%>
					<%= f.input :find, label: "Où peut-on trouver ton alternative" %>
					<%= f.input :source, label: "Lien d'un site qui explique comment fabriquer, ou vend ton alternative" %>
					<%= f.input :imgUrl, label: "L'url d'une image de ton alternative", input_html: {rows: 10, cols: 10}  %>
					<%= f.association :alternative_type_filter, label: "De quel type d'alternative s'agit-il" ,class: 'deroulant-action' %>
				</div>

				<div class="form-actions">
					<%= f.button :submit, 'Envoyer', class: 'btn btn-primary' %>
				</div>
			<% end %>

    </div>
  </div>
</div>