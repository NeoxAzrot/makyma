<% if can? :manage, @atlernative %>

	<h1>Editing Alternative</h1>

	<%= render 'form', alternative: @alternative %>

	<%= link_to 'Show', @alternative %> |
	<%= link_to 'Back', alternatives_path %>
<% end %>
